name: Deploy to Vercel

on:
  push:
    branches:
      - claude/youthful-clarke-6e6d1g

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deploy Hook
        run: |
          curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"
