name: Deploy divulgación física a GitHub Pages

on:
  push:
    branches:
      - claude/laughing-brown-qqa2ya
    paths:
      - "divulgacion-fisica/**"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: divulgacion-fisica

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
